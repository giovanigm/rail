name: rail
description: A lightweight Flutter MVVM library for state and side effects management.
homepage: https://github.com/giovanigm/rail
repository: https://github.com/giovanigm/rail
issue_tracker: https://github.com/giovanigm/rail/issues

version: 1.0.0-dev.1

topics:
  - state
  - effects
  - mvvm
  - view-model

environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
